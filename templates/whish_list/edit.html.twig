<!DOCTYPE html>

<title>Edit WhishList</title>

{% block body %}
    <h1>Edit WhishList</h1>

    {{ include('whish_list/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('whish_list_index') }}">back to list</a>

    {{ include('whish_list/_delete_form.html.twig') }}
{% endblock %}
