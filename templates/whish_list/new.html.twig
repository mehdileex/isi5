<!DOCTYPE html>

<title>New WhishList</title>

{% block body %}
    <h1>Create new WhishList</h1>

    {{ include('whish_list/_form.html.twig') }}

    <a href="{{ path('whish_list_index') }}">back to list</a>
{% endblock %}
