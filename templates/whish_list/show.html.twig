<!DOCTYPE html>

<title>WhishList</title>

{% block body %}
    <h1>WhishList</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ whish_list.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('whish_list_index') }}">back to list</a>

    <a href="{{ path('whish_list_edit', {'id': whish_list.id}) }}">edit</a>

    {{ include('whish_list/_delete_form.html.twig') }}
{% endblock %}
